package com.example.inversionestrack.data.model

enum class RiskProfile(val displayName: String) {
    CONSERVATIVE(displayName = "Conservativo"),
    MODERATE(displayName = "Moderado"),
    AGGRESSIVE(displayName = "Agresivo")
}