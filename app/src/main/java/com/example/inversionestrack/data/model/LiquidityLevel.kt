package com.example.inversionestrack.data.model

enum class LiquidityLevel (val displayName: String){

    HIGH(displayName = "Alto"),
    MEDIUM(displayName = "Medio"),
    LOW(displayName = "Bajo")
}